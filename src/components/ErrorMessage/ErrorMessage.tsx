const ErrorMessage = ({ error }) => {
  return (
    <div>
      <p>
        Some Error Occured <b>&quot;{error}&quot;</b>, Please Try Again
      </p>
    </div>
  );
};

export default ErrorMessage;
